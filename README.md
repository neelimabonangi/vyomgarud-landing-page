# VyomGarud Landing Page

## Setup
1. Install deps: `npm install`
2. Run dev server: `npm run dev`
3. Build: `npm run build`

## Stack
- React + Vite
- TailwindCSS
- Dark, modern, military-inspired design

## Deployment
Deploy using Vercel, Netlify, or Render.

